<Window x:Class="NoOpRunner.Client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ViewModels="clr-namespace:NoOpRunner.Client.Logic.ViewModels;assembly=NoOpRunner.Client.Logic"
        xmlns:local="clr-namespace:NoOpRunner.Client"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <ViewModels:MainViewModel/>
    </Window.DataContext>
    <Grid>
        
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="30"/>
            <!-- <RowDefinition Height="*"/> -->
            <!-- <RowDefinition Height="*"/> -->
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Canvas Grid.Row="0" Grid.ColumnSpan="4" x:Name="map_window"/>
        <Canvas Grid.Row="0" Grid.ColumnSpan="4" x:Name="player_one_window"/>

        <Button Grid.Row="1" Grid.Column="0" Command="{Binding StartHostCommand}" IsEnabled="{Binding HostConnectButtonsEnabled}">Host</Button>
        <Button Grid.Row="1" Grid.Column="1" Command="{Binding ConnectToHostCommand}" IsEnabled="{Binding HostConnectButtonsEnabled}">Connect</Button>
        <Button Grid.Row="1" Grid.Column="2" Command="{Binding SendMessageCommand}">Send message</Button>

        <TextBlock Grid.Row="1" Grid.Column="3" Text="{Binding StatusMessage}" TextAlignment="Center" TextWrapping="Wrap"/>
        
    </Grid>
</Window>
